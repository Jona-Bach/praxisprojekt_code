docker compose up -d ollama
docker compose ps
docker compose logs ollama
docker compose exec ollama ollama list
export OLLAMA_HOST=http://localhost:11434
docker compose exec ollama ollama pull phi3:
docker compose build
docker build -t praxisprojekt:latest .
docker run praxisprojekt:latest
docker image ls 
http://localhost:8501
export OLLAMA_HOST=0.0.0.0:11434
ollama serve
docker compose build app - Nur app bauen
docker compose build app --no-cache

# 1) Nicht genutzte Container/Images/Netzwerke/Volumes aufräumen
docker system prune -a --volumes

# 2) Build-Cache komplett leeren
docker builder prune -a

# 3) (optional/zusätzlich) explizit ungenutzte Volumes weg
docker volume prune
docker system df

# 1) Git-Verzeichnis löschen (macht aus deinem Ordner ein unversioniertes Projekt)
rm -rf .git

# 2) Neu initialisieren und erster Commit
git init
git add -A
git commit -m "Initial commit"
git remote -v 
git remote remove origin
git remote add origin https://github.com/Jona-Bach/praxisprojekt_code.git
git push -u origin main --force
https://github.com/Jona-Bach/praxisprojekt_code.git